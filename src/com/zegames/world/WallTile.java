package com.zegames.world;

import java.awt.image.BufferedImage;

public class WallTile extends Tile{
    public WallTile(BufferedImage sprite, int x, int y, Camera camera) {
        super(sprite, x, y, camera);
    }
}
