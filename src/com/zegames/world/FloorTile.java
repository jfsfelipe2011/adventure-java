package com.zegames.world;

import java.awt.image.BufferedImage;

public class FloorTile extends Tile{
    public FloorTile(BufferedImage sprite, int x, int y, Camera camera) {
        super(sprite, x, y, camera);
    }
}
